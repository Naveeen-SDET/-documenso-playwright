name: Nightly Regression

on:
  # Runs automatically every night at midnight UTC
  # cron format: minute hour day-of-month month day-of-week
  # '0 0 * * *' = at 00:00, every day
  schedule:
    - cron: '0 0 * * *'

  # Allows manual trigger from the GitHub Actions UI
  # Go to Actions tab → Nightly Regression → Run workflow
  workflow_dispatch:

# ── Shared setup steps (reused across all jobs via YAML anchor) ──────────────
# Each job spins up its own fresh Ubuntu machine and starts its own Docker stack.
# This means all 3 jobs run IN PARALLEL — total time = slowest single job.

jobs:

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 1 — API Tests
  # Tests: auth guards, schema validation, pagination, CRUD, boundary conditions
  # CI safe: yes — uses public endpoints, no pre-seeded accounts needed
  # ════════════════════════════════════════════════════════════════════════════
  api-tests:
    name: API Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps

      - name: Clone Documenso app
        run: git clone https://github.com/documenso/documenso.git documenso-app --depth=1

      - name: Start Documenso
        run: docker compose -f documenso-app/docker/testing/compose.yml up -d

      - name: Wait for app to be ready
        run: |
          echo "Waiting for http://localhost:3000..."
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000 > /dev/null && echo "App is up!" && break
            echo "Attempt $i/30 — retrying in 5s..."
            sleep 5
          done

      - name: Run API tests
        run: pnpm exec playwright test tests/api/ --project=ci --reporter=list
        env:
          BASE_URL: http://localhost:3000
          SENDER_EMAIL: sender@test.com
          SENDER_PASSWORD: Test1234!
          SIGNER_EMAIL: signer@test.com
          SIGNER_PASSWORD: Test1234!
          INBUCKET_URL: http://localhost:9000

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: api-test-report
          path: playwright-report/
          retention-days: 7

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 2 — Security Tests
  # Tests: auth guards, token validation, protected UI routes, tampered tokens
  # CI safe: yes — all tests use unauthenticated requests or empty storage state
  # ════════════════════════════════════════════════════════════════════════════
  security-tests:
    name: Security Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps

      - name: Clone Documenso app
        run: git clone https://github.com/documenso/documenso.git documenso-app --depth=1

      - name: Start Documenso
        run: docker compose -f documenso-app/docker/testing/compose.yml up -d

      - name: Wait for app to be ready
        run: |
          echo "Waiting for http://localhost:3000..."
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000 > /dev/null && echo "App is up!" && break
            echo "Attempt $i/30 — retrying in 5s..."
            sleep 5
          done

      - name: Run security tests
        run: pnpm exec playwright test tests/security/ --project=ci --reporter=list
        env:
          BASE_URL: http://localhost:3000
          SENDER_EMAIL: sender@test.com
          SENDER_PASSWORD: Test1234!
          SIGNER_EMAIL: signer@test.com
          SIGNER_PASSWORD: Test1234!
          INBUCKET_URL: http://localhost:9000

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: security-test-report
          path: playwright-report/
          retention-days: 7

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 3 — Accessibility Tests
  # Tests: WCAG 2.1 AA compliance, known violations baseline, new violation gate
  # CI safe: yes — axe-core runs against the public signin and dashboard pages
  # ════════════════════════════════════════════════════════════════════════════
  a11y-tests:
    name: Accessibility Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps

      - name: Clone Documenso app
        run: git clone https://github.com/documenso/documenso.git documenso-app --depth=1

      - name: Start Documenso
        run: docker compose -f documenso-app/docker/testing/compose.yml up -d

      - name: Wait for app to be ready
        run: |
          echo "Waiting for http://localhost:3000..."
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000 > /dev/null && echo "App is up!" && break
            echo "Attempt $i/30 — retrying in 5s..."
            sleep 5
          done

      - name: Run accessibility tests
        run: pnpm exec playwright test tests/accessibility/ --project=ci --reporter=list
        env:
          BASE_URL: http://localhost:3000
          SENDER_EMAIL: sender@test.com
          SENDER_PASSWORD: Test1234!
          SIGNER_EMAIL: signer@test.com
          SIGNER_PASSWORD: Test1234!
          INBUCKET_URL: http://localhost:9000

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: a11y-test-report
          path: playwright-report/
          retention-days: 7

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 4 — Contract Tests (Zod schema validation — positive + negative)
  # Tests: schema shape, negative contract proofs, strict mode, live API contract
  # CI safe: yes — negative tests use constructed objects, no pre-seeded accounts
  # ════════════════════════════════════════════════════════════════════════════
  contract-tests:
    name: Contract Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium --with-deps

      - name: Clone Documenso app
        run: git clone https://github.com/documenso/documenso.git documenso-app --depth=1

      - name: Start Documenso
        run: docker compose -f documenso-app/docker/testing/compose.yml up -d

      - name: Wait for app to be ready
        run: |
          echo "Waiting for http://localhost:3000..."
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000 > /dev/null && echo "App is up!" && break
            echo "Attempt $i/30 — retrying in 5s..."
            sleep 5
          done

      - name: Run contract tests
        run: pnpm exec playwright test tests/api/contracts.spec.ts tests/api/contracts-negative.spec.ts --project=ci --reporter=list
        env:
          BASE_URL: http://localhost:3000
          SENDER_EMAIL: sender@test.com
          SENDER_PASSWORD: Test1234!
          SIGNER_EMAIL: signer@test.com
          SIGNER_PASSWORD: Test1234!
          INBUCKET_URL: http://localhost:9000

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: contract-test-report
          path: playwright-report/
          retention-days: 7

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 5 — Cross-Browser Tests (Firefox)
  # Tests: signin page, auth redirects, JS errors, page structure
  # CI safe: yes — all unauthenticated, no pre-seeded accounts needed
  # ════════════════════════════════════════════════════════════════════════════
  cross-browser-tests:
    name: Cross-Browser Tests (Firefox)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Playwright browsers
        run: pnpm exec playwright install chromium firefox --with-deps

      - name: Clone Documenso app
        run: git clone https://github.com/documenso/documenso.git documenso-app --depth=1

      - name: Start Documenso
        run: docker compose -f documenso-app/docker/testing/compose.yml up -d

      - name: Wait for app to be ready
        run: |
          echo "Waiting for http://localhost:3000..."
          for i in $(seq 1 30); do
            curl -sf http://localhost:3000 > /dev/null && echo "App is up!" && break
            echo "Attempt $i/30 — retrying in 5s..."
            sleep 5
          done

      - name: Run cross-browser tests
        run: pnpm exec playwright test tests/smoke/cross-browser.spec.ts --project=firefox --reporter=list
        env:
          BASE_URL: http://localhost:3000

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: cross-browser-test-report
          path: playwright-report/
          retention-days: 7
